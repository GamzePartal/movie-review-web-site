import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ea3a3aff" // butonun ana rengi
    },
    background: {
      default: "#ffffffff" // sayfanın arka planı
    }
  }
});

export default theme;
